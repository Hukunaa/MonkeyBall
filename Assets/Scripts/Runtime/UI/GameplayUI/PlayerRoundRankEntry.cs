using TMPro;
using UnityEngine;

namespace UI.GameplayUI
{
    public class PlayerRoundRankEntry : MonoBehaviour
    {
        [SerializeField] 
        private TMP_Text _rank_tmp;
    
        [SerializeField] 
        private TMP_Text _playerName_tmp;
    
        [SerializeField] 
        private TMP_Text _playerPerformance_tmp;
    
   
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
